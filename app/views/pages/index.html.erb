<h1>Listing pages</h1>

<table>
  <thead>
    <tr>
      <th>Markdown</th>
      <th>Slug</th>
      <th>Past slugs</th>
      <th>Metadata</th>
      <th>Title</th>
      <th>Template</th>
      <th>Html</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @pages.each do |page| %>
      <tr>
        <td><%= page.markdown %></td>
        <td><%= page.slug %></td>
        <td><%= page.past_slugs %></td>
        <td><%= page.metadata %></td>
        <td><%= page.title %></td>
        <td><%= page.template %></td>
        <td><%= page.html %></td>
        <td><%= link_to 'Show', page %></td>
        <td><%= link_to 'Edit', edit_page_path(page) %></td>
        <td><%= link_to 'Destroy', page, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Page', new_page_path %>
